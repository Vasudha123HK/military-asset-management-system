# Server Configuration
PORT=5000

# JWT Secret Key (change this to a strong random string in production)
JWT_SECRET=your_jwt_secret_key_here

# PostgreSQL Database URL (optional - leave blank to use in-memory database)
# DATABASE_URL=postgresql://username:password@localhost:5432/military_db
